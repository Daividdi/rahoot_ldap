node_modules
.git
# build outputs
.next
dist
# heavy offline packages (the extracted .pnpm-store DOES enter the context;
# offline/bin too, since the Dockerfile uses the pnpm tarball)
offline/deps
offline/images
offline/assets

# exclude nested workspace node_modules (pnpm monorepo) so the image uses the in-Docker install
**/node_modules
